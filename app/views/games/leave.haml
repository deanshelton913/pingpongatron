#games_leave.grid
  .unit.whole
    %h2
      Abandon your current game?
  .unit.whole
    %p
      No score will be recorded, it will be like it never happened.
  .unit.whole
    = form_for @game, html:{ id:'destroy_game', method: :delete} do
      %a.button{href: "#", onclick:'$("#destroy_game").submit();'}
        %span 
          Quit!